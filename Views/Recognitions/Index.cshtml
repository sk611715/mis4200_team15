@model IEnumerable<mis4200_team15.Models.Recognitions>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table table-striped">
    <tr>
        <th>
            Employee Name
        </th>
        <th>
            @Html.DisplayNameFor(model => model.recognitionDate)    
        </th>
        <th>
            @Html.DisplayNameFor(model => model.coreValues.valueName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Description)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.userDetails.fullUserName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.recognitionDate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.coreValues.valueName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Description)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.recognitionsID }) |
            @Html.ActionLink("Details", "Details", new { id=item.recognitionsID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.recognitionsID })
        </td>
    </tr>
}

</table>
